--订单表：新增扣费手机号列（telephone），短信接入号列（commandid），渠道短代码列（channelShortCode）

alter table t_payplatform_order add telephone varchar2(20);
alter table t_payplatform_order add commandid varchar2(25);
alter table t_payplatform_order add channelShortCode varchar2(8);